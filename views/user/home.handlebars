<!DOCTYPE html>
<html>
<head>
    <title>Product List</title>
</head>
<body style="background-color: rgb(20, 46, 67);color:white" >
    <div class="container mt-5">
        <header class="text-center">
            <h1 class="display-4">Welcome, {{customer}}!</h1>
        </header>
        <main class="text-center">
            <p class="lead">This is your home page. You are logged in.</p>
            <div class="alert alert-info">
                <p>Welcome to our website. Here, you can explore various features and services.</p>
            </div>
            <div class="row mt-4">
                <div class="col-md-12">
                    <h2>Products List</h2>
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>No.</th>
                                <th>Product Name</th>
                                <th>Price</th>
                                <th>Available</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each products}}
                            <tr>
                                <td>{{this.no}}</td>
                                <td>{{this.product}}</td>
                                <td>&#8377;{{this.price}}</td>
                                <td>{{this.available}}</td>
                            </tr>
                            {{/each}}
                            <!-- Add more product rows as needed -->
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
        <footer class="text-center mt-4">
            <form action="logout" method="get">
                <button type="submit" class="btn btn-danger">Logout</button>
            </form>
        </footer>
    </div>
</body>
</html>
